export {
  registerModule,
  getModules,
  buildRoutes,
  buildNav,
  resetRegistry,
} from './registry';
export { moduleQueryKey } from './queryKeys';
export type { ModuleDefinition, ModuleInitContext, NavItem, NavChild } from './types';
